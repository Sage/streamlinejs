#!/usr/bin/env node

require('streamline');

var path = require('path');

process.argv.shift();

if (process.argv.length > 1)
	require(path.join(process.cwd(), process.argv[1]));
else
	require("repl").start("node-streamline> ");
